[pytest]
minversion = 7.0
addopts = -v --strict-markers
testpaths = tests

markers =
    integration: tests that use real filesystem/network (run with -m integration)
    network: tests that require internet access
